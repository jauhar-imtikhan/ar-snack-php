on:
  push:
    branches:
      - master
name: 🚀 Deploy Project Ar Snack
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.byethost3.com
          username: b3_35649113
          password: Assword_1234
          server-dir: /htdocs/
